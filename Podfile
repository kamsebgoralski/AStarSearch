# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AStarSearch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AStarSearch
  pod 'SwiftLint'

  target 'AStarSearchTests' do
    # Pods for testing
  end

end

target 'AStarSearchPackageDescription' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AStarSearchPackageDescription

end
